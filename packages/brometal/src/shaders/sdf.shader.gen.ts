/* Generated by BroMetal. Do not edit — recompile with `npx brometal dev`. */
import type { CompiledShader } from '../index.js';

const sdfShader: CompiledShader<{ aPosition: 'vec3'; aUv: 'vec2' }, Record<string, never>, { uTime: 'float'; uAspect: 'float' }> = {
  vertexSrc: `#version 300 es
layout(location = 0) in vec3 aPosition;
layout(location = 1) in vec2 aUv;
out vec2 vUv;
void main() {
  vUv = aUv;
  gl_Position = vec4(aPosition, 1.0);
}
`,
  fragmentSrc: `#version 300 es
precision highp float;
uniform float uTime;
uniform float uAspect;
in vec2 vUv;
out vec4 fragColor;
float sdCircle(vec2 p, float radius) {
  return length(p) - radius;
}
float sdBox2(vec2 p, vec2 halfSize) {
  vec2 d = vec2(abs(p.x) - halfSize.x, abs(p.y) - halfSize.y);
  float outside = length(vec2(max(d.x, 0.0), max(d.y, 0.0)));
  return outside + min(max(d.x, d.y), 0.0);
}
float smoothUnion(float d1, float d2, float k) {
  float h = clamp(0.5 + 0.5 * (d2 - d1) / k, 0.0, 1.0);
  return mix(d2, d1, h) - k * h * (1.0 - h);
}
float fillAA(float d, float softness) {
  return 1.0 - smoothstep(0.0, softness, d);
}
void main() {
  vec2 p = vec2((vUv.x - 0.5) * uAspect, vUv.y - 0.5);
  vec2 orbit = vec2(cos(uTime) * 0.22, sin(uTime * 1.3) * 0.18);
  float circle = sdCircle(p - orbit, 0.12);
  float box = sdBox2(p, vec2(0.16, 0.16));
  float d = smoothUnion(circle, box, 0.09);
  float fill = fillAA(d, 0.006);
  float edge = fillAA(abs(d) - 0.004, 0.004);
  vec3 body = vec3(0.98, 0.45, 0.2) * fill;
  fragColor = vec4(body + vec3(1.0, 0.9, 0.6) * (edge * 0.6), 1.0);
}
`,
  wgslSrc: `struct BmUniforms {
  uTime : f32,
  uAspect : f32,
}
@group(0) @binding(0) var<uniform> bm_u : BmUniforms;
struct BmVSIn {
  @location(0) aPosition : vec3f,
  @location(1) aUv : vec2f,
}
struct BmVSOut {
  @builtin(position) bm_position : vec4f,
  @location(0) vUv : vec2f,
}
fn sdCircle(p : vec2f, radius : f32) -> f32 {
  return length(p) - radius;
}
fn sdBox2(p : vec2f, halfSize : vec2f) -> f32 {
  let d = vec2f(abs(p.x) - halfSize.x, abs(p.y) - halfSize.y);
  let outside = length(vec2f(max(d.x, 0.0), max(d.y, 0.0)));
  return outside + min(max(d.x, d.y), 0.0);
}
fn smoothUnion(d1 : f32, d2 : f32, k : f32) -> f32 {
  let h = clamp(0.5 + 0.5 * (d2 - d1) / k, 0.0, 1.0);
  return mix(d2, d1, h) - k * h * (1.0 - h);
}
fn fillAA(d : f32, softness : f32) -> f32 {
  return 1.0 - smoothstep(0.0, softness, d);
}
@vertex
fn vs_main(bm_in : BmVSIn) -> BmVSOut {
  var bm_out : BmVSOut;
  bm_out.vUv = bm_in.aUv;
  bm_out.bm_position = vec4f(bm_in.aPosition, 1.0);
  bm_out.bm_position.z = (bm_out.bm_position.z + bm_out.bm_position.w) * 0.5;
  return bm_out;
}
@fragment
fn fs_main(bm_in : BmVSOut) -> @location(0) vec4f {
  let p = vec2f((bm_in.vUv.x - 0.5) * bm_u.uAspect, bm_in.vUv.y - 0.5);
  let orbit = vec2f(cos(bm_u.uTime) * 0.22, sin(bm_u.uTime * 1.3) * 0.18);
  let circle = sdCircle(p - orbit, 0.12);
  let box = sdBox2(p, vec2f(0.16, 0.16));
  let d = smoothUnion(circle, box, 0.09);
  let fill = fillAA(d, 0.006);
  let edge = fillAA(abs(d) - 0.004, 0.004);
  let body = vec3f(0.98, 0.45, 0.2) * fill;
  return vec4f(body + vec3f(1.0, 0.9, 0.6) * (edge * 0.6), 1.0);
}
`,
  attributes: { aPosition: 'vec3', aUv: 'vec2' },
  instanceAttributes: {},
  uniforms: { uTime: 'float', uAspect: 'float' },
  layout: {"attributes":[{"name":"aPosition","type":"vec3","location":0,"size":3,"divisor":0},{"name":"aUv","type":"vec2","location":1,"size":2,"divisor":0}],"uniforms":[{"name":"uTime","type":"float","kind":"1f","size":1,"offset":0},{"name":"uAspect","type":"float","kind":"1f","size":1,"offset":4}],"uniformBlockSize":16},
};

export default sdfShader;
