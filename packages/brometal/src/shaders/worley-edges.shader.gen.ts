/* Generated by BroMetal. Do not edit — recompile with `npx brometal dev`. */
import type { CompiledShader } from '../index.js';

const worleyEdgesShader: CompiledShader<{ aPosition: 'vec3'; aUv: 'vec2' }, Record<string, never>, { uTime: 'float'; uAspect: 'float' }> = {
  vertexSrc: `#version 300 es
layout(location = 0) in vec3 aPosition;
layout(location = 1) in vec2 aUv;
out vec2 vUv;
void main() {
  vUv = aUv;
  gl_Position = vec4(aPosition, 1.0);
}
`,
  fragmentSrc: `#version 300 es
precision highp float;
uniform float uTime;
uniform float uAspect;
in vec2 vUv;
out vec4 fragColor;
vec2 hash22(vec2 p) {
  vec2 k = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));
  return vec2(fract(sin(k.x) * 43758.5453), fract(sin(k.y) * 43758.5453));
}
float worleyEdge2(vec2 p) {
  vec2 cell = vec2(floor(p.x), floor(p.y));
  vec2 f = p - cell;
  float f1 = 8.0;
  float f2 = 8.0;
  for (float i = -1.0; i <= 1.0; i = i + 1.0) {
    for (float j = -1.0; j <= 1.0; j = j + 1.0) {
      vec2 neighbor = vec2(i, j);
      vec2 feature = hash22(cell + neighbor);
      float d = length(neighbor + feature - f);
      if (d < f1) {
        f2 = f1;
        f1 = d;
      } else {
        if (d < f2) {
          f2 = d;
        }
      }
    }
  }
  return f2 - f1;
}
void main() {
  vec2 q = vec2(vUv.x * uAspect, vUv.y) * 5.0 + vec2(uTime * 0.15, uTime * 0.1);
  float e = worleyEdge2(q);
  float line = 1.0 - smoothstep(0.0, 0.08, e);
  float cellGlow = pow(1.0 - smoothstep(0.0, 0.6, e), 2.0) * 0.25;
  vec3 base = vec3(0.06, 0.04, 0.12);
  vec3 edgeColor = vec3(0.5, 1.0, 0.85);
  fragColor = vec4(mix(base, edgeColor, line) + edgeColor * cellGlow, 1.0);
}
`,
  wgslSrc: `struct BmUniforms {
  uTime : f32,
  uAspect : f32,
}
@group(0) @binding(0) var<uniform> bm_u : BmUniforms;
struct BmVSIn {
  @location(0) aPosition : vec3f,
  @location(1) aUv : vec2f,
}
struct BmVSOut {
  @builtin(position) bm_position : vec4f,
  @location(0) vUv : vec2f,
}
fn hash22(p : vec2f) -> vec2f {
  let k = vec2f(dot(p, vec2f(127.1, 311.7)), dot(p, vec2f(269.5, 183.3)));
  return vec2f(fract(sin(k.x) * 43758.5453), fract(sin(k.y) * 43758.5453));
}
fn worleyEdge2(p : vec2f) -> f32 {
  let cell = vec2f(floor(p.x), floor(p.y));
  let f = p - cell;
  var f1 = 8.0;
  var f2 = 8.0;
  for (var i = -1.0; i <= 1.0; i = i + 1.0) {
    for (var j = -1.0; j <= 1.0; j = j + 1.0) {
      let neighbor = vec2f(i, j);
      let feature = hash22(cell + neighbor);
      let d = length(neighbor + feature - f);
      if (d < f1) {
        f2 = f1;
        f1 = d;
      } else {
        if (d < f2) {
          f2 = d;
        }
      }
    }
  }
  return f2 - f1;
}
@vertex
fn vs_main(bm_in : BmVSIn) -> BmVSOut {
  var bm_out : BmVSOut;
  bm_out.vUv = bm_in.aUv;
  bm_out.bm_position = vec4f(bm_in.aPosition, 1.0);
  bm_out.bm_position.z = (bm_out.bm_position.z + bm_out.bm_position.w) * 0.5;
  return bm_out;
}
@fragment
fn fs_main(bm_in : BmVSOut) -> @location(0) vec4f {
  let q = vec2f(bm_in.vUv.x * bm_u.uAspect, bm_in.vUv.y) * 5.0 + vec2f(bm_u.uTime * 0.15, bm_u.uTime * 0.1);
  let e = worleyEdge2(q);
  let line = 1.0 - smoothstep(0.0, 0.08, e);
  let cellGlow = pow(1.0 - smoothstep(0.0, 0.6, e), 2.0) * 0.25;
  let base = vec3f(0.06, 0.04, 0.12);
  let edgeColor = vec3f(0.5, 1.0, 0.85);
  return vec4f(mix(base, edgeColor, line) + edgeColor * cellGlow, 1.0);
}
`,
  attributes: { aPosition: 'vec3', aUv: 'vec2' },
  instanceAttributes: {},
  uniforms: { uTime: 'float', uAspect: 'float' },
  layout: {"attributes":[{"name":"aPosition","type":"vec3","location":0,"size":3,"divisor":0},{"name":"aUv","type":"vec2","location":1,"size":2,"divisor":0}],"uniforms":[{"name":"uTime","type":"float","kind":"1f","size":1,"offset":0},{"name":"uAspect","type":"float","kind":"1f","size":1,"offset":4}],"uniformBlockSize":16},
};

export default worleyEdgesShader;
