/* Generated by BroMetal. Do not edit — recompile with `npx brometal dev`. */
import type { CompiledShader } from 'brometal';

const gameRocksShader: CompiledShader<{ aPosition: 'vec3'; aUv: 'vec2' }, { iOffset: 'vec3'; iScale: 'float'; iSeed: 'float' }, { uViewProj: 'mat4'; uScroll: 'float'; uWrap: 'float'; uAhead: 'float'; uLightDir: 'vec3'; uTime: 'float'; uTex: 'sampler2D'; uEnginePos: 'vec3'; uEngineColor: 'vec3'; uPulse: 'float'; uBolts: 'storage' }> = {
  wgslSrc: `struct BmUniforms {
  uViewProj : mat4x4f,
  uScroll : f32,
  uWrap : f32,
  uAhead : f32,
  uLightDir : vec3f,
  uTime : f32,
  uEnginePos : vec3f,
  uEngineColor : vec3f,
  uPulse : f32,
}
@group(0) @binding(0) var<uniform> bm_u : BmUniforms;
@group(0) @binding(1) var uTex : texture_2d<f32>;
@group(0) @binding(2) var uTex_sampler : sampler;
@group(0) @binding(3) var<storage, read> uBolts : array<vec4f>;
struct BmVSIn {
  @location(0) aPosition : vec3f,
  @location(1) aUv : vec2f,
  @location(2) iOffset : vec3f,
  @location(3) iScale : f32,
  @location(4) iSeed : f32,
}
struct BmVSOut {
  @builtin(position) bm_position : vec4f,
  @location(0) vNormal : vec3f,
  @location(1) vUv : vec2f,
  @location(2) vTint : f32,
  @location(3) vHeight : f32,
  @location(4) vDepth : f32,
  @location(5) vWorldPos : vec3f,
}
fn rotate2(p : vec2f, angle : f32) -> vec2f {
  let c = cos(angle);
  let s = sin(angle);
  return vec2f(p.x * c - p.y * s, p.x * s + p.y * c);
}
fn hash11(p : f32) -> f32 {
  return fract(sin(p * 127.1) * 43758.5453);
}
fn lambert(normal : vec3f, lightDir : vec3f) -> f32 {
  return max(dot(normalize(normal), normalize(lightDir)), 0.0);
}
fn hash31(p : vec3f) -> f32 {
  return fract(sin(dot(p, vec3f(127.1, 311.7, 74.7))) * 43758.5453);
}
fn vnoise3(p : vec3f) -> f32 {
  let cell = vec3f(floor(p.x), floor(p.y), floor(p.z));
  let f = p - cell;
  let u = vec3f(f.x * f.x * (3.0 - 2.0 * f.x), f.y * f.y * (3.0 - 2.0 * f.y), f.z * f.z * (3.0 - 2.0 * f.z));
  let n000 = hash31(cell);
  let n100 = hash31(cell + vec3f(1.0, 0.0, 0.0));
  let n010 = hash31(cell + vec3f(0.0, 1.0, 0.0));
  let n110 = hash31(cell + vec3f(1.0, 1.0, 0.0));
  let n001 = hash31(cell + vec3f(0.0, 0.0, 1.0));
  let n101 = hash31(cell + vec3f(1.0, 0.0, 1.0));
  let n011 = hash31(cell + vec3f(0.0, 1.0, 1.0));
  let n111 = hash31(cell + vec3f(1.0, 1.0, 1.0));
  let nx00 = mix(n000, n100, u.x);
  let nx10 = mix(n010, n110, u.x);
  let nx01 = mix(n001, n101, u.x);
  let nx11 = mix(n011, n111, u.x);
  let nxy0 = mix(nx00, nx10, u.y);
  let nxy1 = mix(nx01, nx11, u.y);
  return mix(nxy0, nxy1, u.z);
}
fn fbm3(p : vec3f, octaves : f32) -> f32 {
  var total = 0.0;
  var amplitude = 0.5;
  var frequency = 1.0;
  var norm = 0.0;
  for (var i = 0.0; i < octaves; i = i + 1.0) {
    total = total + amplitude * vnoise3(p * frequency);
    norm = norm + amplitude;
    amplitude = amplitude * 0.5;
    frequency = frequency * 2.0;
  }
  return total / norm;
}
fn craters(dir : vec3f, seed : f32) -> f32 {
  var acc = 0.0;
  for (var i = 0.0; i < 14.0; i = i + 1.0) {
    let base = seed * 31.7 + i * 13.9;
    let azimuth = hash11(base) * 6.2831853;
    let cosPolar = hash11(base + 4.31) * 2.0 - 1.0;
    let sinPolar = sqrt(max(1.0 - cosPolar * cosPolar, 0.0));
    let centre = vec3f(cos(azimuth) * sinPolar, cosPolar, sin(azimuth) * sinPolar);
    let angle = acos(clamp(dot(dir, centre), -1.0, 1.0));
    let radius = 0.11 + hash11(base + 7.77) * 0.26;
    let d = angle / radius;
    let bowl = (d * d - 1.0) * (1.0 - smoothstep(0.7, 1.0, d));
    let rim = exp(0.0 - (d - 1.0) * (d - 1.0) * 42.0);
    acc = acc + bowl * 0.085 + rim * 0.05;
  }
  return acc;
}
fn rockRadius(dir : vec3f, seed : f32) -> f32 {
  let q = dir * 2.3 + vec3f(seed * 37.7, seed * 11.3, seed * 71.9);
  return 1.0 + (fbm3(q, 4.0) - 0.5) * 0.4 + craters(dir, seed);
}
@vertex
fn vs_main(bm_in : BmVSIn) -> BmVSOut {
  var bm_out : BmVSOut;
  let dir = normalize(bm_in.aPosition);
  let t1 = normalize(cross(dir, vec3f(0.31, 0.82, 0.47)));
  let t2 = cross(dir, t1);
  let e = 0.12;
  let d1 = normalize(dir + t1 * e);
  let d2 = normalize(dir + t2 * e);
  let r0 = rockRadius(dir, bm_in.iSeed);
  let p0 = dir * r0;
  let edge1 = d1 * rockRadius(d1, bm_in.iSeed) - p0;
  let edge2 = d2 * rockRadius(d2, bm_in.iSeed) - p0;
  let normal = normalize(cross(edge1, edge2));
  let spin = bm_u.uTime * (0.2 + bm_in.iSeed * 0.8);
  let pr = rotate2(p0.xz, spin);
  let nr = rotate2(normal.xz, spin);
  let local = vec3f(pr.x, p0.y, pr.y) * bm_in.iScale;
  let z = ((bm_in.iOffset.z + bm_u.uScroll) - (bm_u.uWrap) * floor((bm_in.iOffset.z + bm_u.uScroll) / (bm_u.uWrap))) - bm_u.uWrap + bm_u.uAhead;
  let world = local + vec3f(bm_in.iOffset.x, bm_in.iOffset.y, z);
  bm_out.vNormal = vec3f(nr.x, normal.y, nr.y);
  bm_out.vUv = bm_in.aUv * 2.0 + vec2f(bm_in.iSeed * 5.3, bm_in.iSeed * 2.9);
  bm_out.vTint = 0.6 + hash11(bm_in.iSeed * 91.7) * 0.4;
  bm_out.vHeight = r0;
  bm_out.vDepth = z;
  bm_out.vWorldPos = world;
  bm_out.bm_position = bm_u.uViewProj * vec4f(world, 1.0);
  bm_out.bm_position.z = (bm_out.bm_position.z + bm_out.bm_position.w) * 0.5;
  return bm_out;
}
@fragment
fn fs_main(bm_in : BmVSOut) -> @location(0) vec4f {
  let grain = textureSample(uTex, uTex_sampler, bm_in.vUv).xyz;
  let diffuse = lambert(bm_in.vNormal, bm_u.uLightDir);
  let t = clamp((bm_in.vHeight - 0.72) * 1.8, 0.0, 1.0);
  let grade = mix(vec3f(0.55, 0.62, 0.85), vec3f(1.15, 1.15, 1.25), t);
  let base = grain * grade * bm_in.vTint;
  let sun = base * (diffuse * 1.5);
  let toEngine = bm_u.uEnginePos - bm_in.vWorldPos;
  let distance = length(toEngine);
  let engineDir = normalize(toEngine);
  let falloff = bm_u.uPulse / (1.0 + distance * distance * 0.05);
  let engineDiffuse = max(dot(normalize(bm_in.vNormal), engineDir), 0.0) * falloff * 3.2;
  var boltLight = 0.0;
  let bolts = f32(arrayLength(&uBolts));
  for (var i = 0.0; i < bolts; i = i + 1.0) {
    let bolt = uBolts[u32(i)];
    let toBolt = vec3f(bolt.x, bolt.y, bolt.z) - bm_in.vWorldPos;
    let boltDist = length(toBolt);
    let boltFall = bolt.w / (1.0 + boltDist * boltDist * 0.9);
    boltLight = boltLight + max(dot(normalize(bm_in.vNormal), normalize(toBolt)), 0.0) * boltFall;
  }
  let lit = sun + base * bm_u.uEngineColor * engineDiffuse + base * (boltLight * 0.75);
  let haze = vec3f(0.0, 0.0, 0.0);
  let dist = 0.0 - bm_in.vDepth;
  let fog = clamp((dist - 5.0) / 130.0, 0.0, 1.0);
  return vec4f(mix(lit, haze, fog), 1.0);
}
`,
  attributes: { aPosition: 'vec3', aUv: 'vec2' },
  instanceAttributes: { iOffset: 'vec3', iScale: 'float', iSeed: 'float' },
  uniforms: { uViewProj: 'mat4', uScroll: 'float', uWrap: 'float', uAhead: 'float', uLightDir: 'vec3', uTime: 'float', uTex: 'sampler2D', uEnginePos: 'vec3', uEngineColor: 'vec3', uPulse: 'float', uBolts: 'storage' },
  layout: {"attributes":[{"name":"aPosition","type":"vec3","location":0,"size":3,"divisor":0},{"name":"aUv","type":"vec2","location":1,"size":2,"divisor":0},{"name":"iOffset","type":"vec3","location":2,"size":3,"divisor":1},{"name":"iScale","type":"float","location":3,"size":1,"divisor":1},{"name":"iSeed","type":"float","location":4,"size":1,"divisor":1}],"uniforms":[{"name":"uViewProj","type":"mat4","kind":"m4fv","size":16,"offset":0},{"name":"uScroll","type":"float","kind":"1f","size":1,"offset":64},{"name":"uWrap","type":"float","kind":"1f","size":1,"offset":68},{"name":"uAhead","type":"float","kind":"1f","size":1,"offset":72},{"name":"uLightDir","type":"vec3","kind":"3fv","size":3,"offset":80},{"name":"uTime","type":"float","kind":"1f","size":1,"offset":92},{"name":"uTex","type":"sampler2D","kind":"1i","size":1,"unit":0,"textureBinding":1,"samplerBinding":2},{"name":"uEnginePos","type":"vec3","kind":"3fv","size":3,"offset":96},{"name":"uEngineColor","type":"vec3","kind":"3fv","size":3,"offset":112},{"name":"uPulse","type":"float","kind":"1f","size":1,"offset":124},{"name":"uBolts","type":"storage","kind":"1i","size":1,"unit":1,"textureBinding":3}],"uniformBlockSize":128},

};

export default gameRocksShader;
