/* Generated by BroMetal. Do not edit — recompile with `npx brometal dev`. */
import type { CompiledShader } from 'brometal';

const waterAssembleShader: CompiledShader<{ aPosition: 'vec3'; aUv: 'vec2' }, Record<string, never>, { uTransform: 'sampler2D'; uPatchSize: 'float'; uChoppiness: 'float'; uScale: 'float'; uFoamThreshold: 'float' }> = {
  vertexSrc: `#version 300 es
precision highp float;
precision highp int;
layout(location = 0) in vec3 aPosition;
layout(location = 1) in vec2 aUv;
out vec2 vUv;
void main() {
  vUv = aUv;
  gl_Position = vec4(aPosition.x, aPosition.y, 0.0, 1.0);
}
`,
  fragmentSrc: `#version 300 es
precision highp float;
precision highp int;
precision highp sampler2D;
precision highp sampler3D;
uniform sampler2D uTransform;
uniform float uPatchSize;
uniform float uChoppiness;
uniform float uScale;
uniform float uFoamThreshold;
in vec2 vUv;
out vec4 fragColor;
void main() {
  float size = 128.0;
  float x = floor(vUv.x * size);
  float y = floor(vUv.y * size);
  float sign = 1.0 - 2.0 * mod(x + y, 2.0);
  float gain = uScale * sign;
  vec4 centre = texture(uTransform, vUv);
  vec3 displacement = vec3(centre.x * uChoppiness * gain, centre.y * gain, centre.z * uChoppiness * gain);
  float texel = 1.0 / size;
  vec4 right = texture(uTransform, vec2(fract(vUv.x + texel), vUv.y));
  vec4 up = texture(uTransform, vec2(vUv.x, fract(vUv.y + texel)));
  float perMetre = size / uPatchSize;
  float dxdx = (0.0 - right.x * uChoppiness * gain - displacement.x) * perMetre;
  float dzdx = (0.0 - right.z * uChoppiness * gain - displacement.z) * perMetre;
  float dxdz = (0.0 - up.x * uChoppiness * gain - displacement.x) * perMetre;
  float dzdz = (0.0 - up.z * uChoppiness * gain - displacement.z) * perMetre;
  float jacobian = (1.0 + dxdx) * (1.0 + dzdz) - dxdz * dzdx;
  float foam = max(uFoamThreshold - jacobian, 0.0);
  fragColor = vec4(displacement.x, displacement.y, displacement.z, foam);
}
`,
  wgslSrc: `struct BmUniforms {
  uPatchSize : f32,
  uChoppiness : f32,
  uScale : f32,
  uFoamThreshold : f32,
}
@group(0) @binding(0) var<uniform> bm_u : BmUniforms;
@group(0) @binding(1) var uTransform : texture_2d<f32>;
@group(0) @binding(2) var uTransform_sampler : sampler;
struct BmVSIn {
  @location(0) aPosition : vec3f,
  @location(1) aUv : vec2f,
}
struct BmVSOut {
  @builtin(position) bm_position : vec4f,
  @location(0) vUv : vec2f,
}
@vertex
fn vs_main(bm_in : BmVSIn) -> BmVSOut {
  var bm_out : BmVSOut;
  bm_out.vUv = bm_in.aUv;
  bm_out.bm_position = vec4f(bm_in.aPosition.x, bm_in.aPosition.y, 0.0, 1.0);
  bm_out.bm_position.z = (bm_out.bm_position.z + bm_out.bm_position.w) * 0.5;
  return bm_out;
}
@fragment
fn fs_main(bm_in : BmVSOut) -> @location(0) vec4f {
  let size = 128.0;
  let x = floor(bm_in.vUv.x * size);
  let y = floor(bm_in.vUv.y * size);
  let sign = 1.0 - 2.0 * ((x + y) - (2.0) * floor((x + y) / (2.0)));
  let gain = bm_u.uScale * sign;
  let centre = textureSample(uTransform, uTransform_sampler, bm_in.vUv);
  let displacement = vec3f(centre.x * bm_u.uChoppiness * gain, centre.y * gain, centre.z * bm_u.uChoppiness * gain);
  let texel = 1.0 / size;
  let right = textureSample(uTransform, uTransform_sampler, vec2f(fract(bm_in.vUv.x + texel), bm_in.vUv.y));
  let up = textureSample(uTransform, uTransform_sampler, vec2f(bm_in.vUv.x, fract(bm_in.vUv.y + texel)));
  let perMetre = size / bm_u.uPatchSize;
  let dxdx = (0.0 - right.x * bm_u.uChoppiness * gain - displacement.x) * perMetre;
  let dzdx = (0.0 - right.z * bm_u.uChoppiness * gain - displacement.z) * perMetre;
  let dxdz = (0.0 - up.x * bm_u.uChoppiness * gain - displacement.x) * perMetre;
  let dzdz = (0.0 - up.z * bm_u.uChoppiness * gain - displacement.z) * perMetre;
  let jacobian = (1.0 + dxdx) * (1.0 + dzdz) - dxdz * dzdx;
  let foam = max(bm_u.uFoamThreshold - jacobian, 0.0);
  return vec4f(displacement.x, displacement.y, displacement.z, foam);
}
`,
  attributes: { aPosition: 'vec3', aUv: 'vec2' },
  instanceAttributes: {},
  uniforms: { uTransform: 'sampler2D', uPatchSize: 'float', uChoppiness: 'float', uScale: 'float', uFoamThreshold: 'float' },
  layout: {"attributes":[{"name":"aPosition","type":"vec3","location":0,"size":3,"divisor":0},{"name":"aUv","type":"vec2","location":1,"size":2,"divisor":0}],"uniforms":[{"name":"uTransform","type":"sampler2D","kind":"1i","size":1,"unit":0,"textureBinding":1,"samplerBinding":2},{"name":"uPatchSize","type":"float","kind":"1f","size":1,"offset":0},{"name":"uChoppiness","type":"float","kind":"1f","size":1,"offset":4},{"name":"uScale","type":"float","kind":"1f","size":1,"offset":8},{"name":"uFoamThreshold","type":"float","kind":"1f","size":1,"offset":12}],"uniformBlockSize":16},

};

export default waterAssembleShader;
