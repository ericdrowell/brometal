/* Generated by BroMetal. Do not edit — recompile with `npx brometal dev`. */
import type { CompiledShader } from 'brometal';

const LegendOfBroIris: CompiledShader<{ aCorner: 'vec2' }, Record<string, never>, { uScenePx: 'vec2'; uCenter: 'vec2'; uRadius: 'float' }> = {
  wgslSrc: `struct BmUniforms {
  uScenePx : vec2f,
  uCenter : vec2f,
  uRadius : f32,
}
@group(0) @binding(0) var<uniform> bm_u : BmUniforms;
struct BmVSIn {
  @location(0) aCorner : vec2f,
}
struct BmVSOut {
  @builtin(position) bm_position : vec4f,
  @location(0) vUv : vec2f,
}
fn bayer2(x : f32, y : f32) -> f32 {
  return ((2.0 * ((floor(y)) - (2.0) * floor((floor(y)) / (2.0))) + 3.0 * ((floor(x)) - (2.0) * floor((floor(x)) / (2.0)))) - (4.0) * floor((2.0 * ((floor(y)) - (2.0) * floor((floor(y)) / (2.0))) + 3.0 * ((floor(x)) - (2.0) * floor((floor(x)) / (2.0)))) / (4.0)));
}
fn bayer8(x : f32, y : f32) -> f32 {
  let fine = 16.0 * bayer2(x, y);
  let mid = 4.0 * bayer2(floor(x / 2.0), floor(y / 2.0));
  return (fine + mid + bayer2(floor(x / 4.0), floor(y / 4.0))) / 64.0;
}
@vertex
fn vs_main(bm_in : BmVSIn) -> BmVSOut {
  var bm_out : BmVSOut;
  bm_out.vUv = vec2f(bm_in.aCorner.x, 1.0 - bm_in.aCorner.y);
  bm_out.bm_position = vec4f(bm_in.aCorner.x * 2.0 - 1.0, bm_in.aCorner.y * 2.0 - 1.0, 0.0, 1.0);
  bm_out.bm_position.z = (bm_out.bm_position.z + bm_out.bm_position.w) * 0.5;
  return bm_out;
}
@fragment
fn fs_main(bm_in : BmVSOut) -> @location(0) vec4f {
  let px = bm_in.vUv * bm_u.uScenePx;
  let dist = length((bm_in.vUv - bm_u.uCenter) * bm_u.uScenePx);
  let edge = (dist - bm_u.uRadius) / 26.0;
  let hidden = 1.0 - step(edge, bayer8(px.x, px.y));
  return vec4f(0.0, 0.0, 0.0, hidden);
}
`,
  attributes: { aCorner: 'vec2' },
  instanceAttributes: {},
  uniforms: { uScenePx: 'vec2', uCenter: 'vec2', uRadius: 'float' },
  layout: {"attributes":[{"name":"aCorner","type":"vec2","location":0,"size":2,"divisor":0}],"uniforms":[{"name":"uScenePx","type":"vec2","kind":"2fv","size":2,"offset":0},{"name":"uCenter","type":"vec2","kind":"2fv","size":2,"offset":8},{"name":"uRadius","type":"float","kind":"1f","size":1,"offset":16}],"uniformBlockSize":32},

};

export default LegendOfBroIris;
